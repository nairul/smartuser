<h2>New Account for <%= link_to @app.name, @app %></h2>

<%= form_for [@app, @account] do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.text_field :password %>
  
  <%= f.submit %>
<% end %>
