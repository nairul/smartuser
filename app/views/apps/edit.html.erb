<h2>Edit App</h2>

<%= form_for @app do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :app_url %>
  <%= f.text_field :app_url %>

  <%= f.submit %>
<% end %>

<%= link_to "Delete", @app, method: :delete, class: "delete" %>
